module.exports = async (req, res) => {
    // Just add file which is in views folder it will automatically get the path of file
    res.render('dashboard') // Rendering dashboard file on '/' route as a response.
} 